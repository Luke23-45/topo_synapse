# Baseline ablation configuration
# ===============================
# Full model with all components enabled

defaults:
  - ../training/default

ablation:
  disable_proxy: false
  disable_exact_eval: false
  disable_anchor_selection: false
  disable_topology: false

experiment:
  name: synapse_z3_ablation_baseline
  tags: ["z3", "synapse", "ablation", "baseline"]
