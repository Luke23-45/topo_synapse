# Spatial dataset configuration
# ============================
# Overrides for spatial trajectory classification dataset

dataset: spatial

data:
  dataset: spatial
  train_size: null
  val_size: null
  test_size: null
  max_samples: null
  data_root: null

model:
  input_dim: 4
  output_dim: 6
  max_history_tokens: 128

training:
  batch_size: 32
  max_epochs: 30
