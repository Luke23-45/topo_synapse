# Photonic dataset configuration
# ==============================
# Overrides for photonic signal classification dataset

dataset: photonic

data:
  dataset: photonic
  train_size: null
  val_size: null
  test_size: null
  max_samples: null
  data_root: null

model:
  input_dim: 16
  output_dim: 8
  max_history_tokens: 512

training:
  batch_size: 128
  max_epochs: 100
