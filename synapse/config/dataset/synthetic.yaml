# Synthetic Topology Dataset
# =========================
# Heuristic-based synthetic dataset for pipeline validation.
# Z3 alignment: Baseline smoke tests.

data:
  dataset: synthetic
  modality: topological
  source: internal
  input_dim: 2
  sequence_length: 128
  num_classes: 4
  task: classification
  batch_size_override: 32
  data_root: null
