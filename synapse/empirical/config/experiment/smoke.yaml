# Z3 Smoke Test
# ==============
# Quick validation with synthetic dataset only.
# Should complete in under 1 minute.

datasets:
  - synthetic

training:
  epochs: 2
  batch_size: 32
  learning_rate: 0.001
  weight_decay: 0.0001

execution:
  seed: 7

output_dir: synapse_outputs/smoke
