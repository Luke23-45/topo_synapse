# MLP Baseline Overlay — Sanity Check
# Flattens encoded tokens and passes through 3-layer MLP.
# Overlay: only fields that differ from the base experiment config.

backbone: mlp

model:
  hidden_dim: 256

loss:
  proxy_weight: 0.0
  sparsity_weight: 0.0
