# TCN Baseline Overlay — Temporal Baseline
# Dilated causal convolutions with residual connections.
# Overlay: only fields that differ from the base experiment config.

backbone: tcn

model:
  kernel_size: 3

loss:
  proxy_weight: 0.0
  sparsity_weight: 0.0
